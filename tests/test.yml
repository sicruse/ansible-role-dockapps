---
- hosts: localhost
  remote_user: root
  vars_files:
    - test_vars.yml  
  roles:
    - ../..

# To test use this command from the root of the module
# ansible-playbook -i "localhost," -c local tests/test.yml